apiVersion: v1
kind: Service
metadata:
  name: lab-nxrm-nexusrepo
  labels:
    chart: "{{ .Chart.Name }}-{{ .Chart.Version }}"
    app: {{ template "fullname" . }}
  namespace: nxrm
spec:
  ports:
    - name: tcp-nexusrepo
      nodePort: null
      port: 8081
      targetPort: tcp-nexusrepo
  selector:
    app: {{ template "fullname" . }}
  sessionAffinity: None
  type: ClusterIP
